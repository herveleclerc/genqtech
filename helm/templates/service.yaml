apiVersion: v1
kind: Service
metadata:
  name: {{ include "genqtech-app.fullname" . }}
  labels:
    {{- include "genqtech-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "genqtech-app.selectorLabels" . | nindent 4 }}
